/**
 * AudioWorkletProcessor wrapping `core/pitch-engine`.
 *
 * CONTRACT FILE — implementation owned by the integration workstream.
 *
 * This file is the entry point of the IIFE worklet bundle. Everything it pulls
 * in must come from `src/core/`, which is why core has no DOM and no imports.
 */

export {};
