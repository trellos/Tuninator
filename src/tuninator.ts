/**
 * TuninatorImpl: state machine, microphone, AudioContext, emitter, mode policy.
 *
 * CONTRACT FILE — implementation owned by the integration workstream.
 */

import type { Tuninator, TuninatorOptions } from "./types.js";

export function createTuninator(_options: TuninatorOptions = {}): Tuninator {
  throw new Error("createTuninator: not implemented");
}
