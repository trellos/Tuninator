# The runtime ships its CPU binaries inside the package. Its postinstall only
# fetches optional CUDA binaries from a host outside this evaluation, so no
# install script runs here.
ignore-scripts=true
save-exact=true
